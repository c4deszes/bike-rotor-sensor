#include "app/spm.h"
#include "app/iet.h"
#include "bsp/sensor.h"

// external config:
// - channel allocation
// - wheel sizes

void SPM_Initialize(void) {
    // 1. load wheel size
    // 2. 

    
}

void SPM_Update(void) {
    // 3. check osh_phy and sensor errors

    // 1. if we have new speed then process that value
    //

    // 2. check inertial estimate, compensate value with that
}
